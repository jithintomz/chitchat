{
  "name": "chat",
  "description": "all in one chat app",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "start": "node server.js",
    "test": "mocha --reporter spec test"
  },
  "dependencies": {
  },
  "engines": {
    "node": "0.10.x"
  }
}
